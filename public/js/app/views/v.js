import Backbone from 'backbone';
import _ from 'lodash';

export default Backbone.View.extend({

  initialize: function() {
    _.bindAll(this);
  },

  render: function() {
    var view = this;

    return view;
  }

});
